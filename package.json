{
  "name": "aws-app",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": "9.4.0",
    "yarn": "1.3.2"
  },
  "scripts": {
    "flow-all": "(cd client && yarn flow); (cd server && yarn flow)",
    "standard-all": "(cd client && yarn standard); (cd server && yarn standard)",
    "start": "node server/dist/index.js",
    "postinstall": "yarn install --cwd server --production=false && yarn install --cwd client --production=false",
    "heroku-postbuild": "(cd server && yarn build); (cd client && yarn build)"
  }
}
